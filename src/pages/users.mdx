export const description =
  'Users pages'

# Users

Users model is the main application user. {{ className: 'lead' }}

### Properties

<Properties>
  <Property name="id" type="integer">
    Unique identifier for the user.
  </Property>
  <Property name="name" type="string">
    The user name.
  </Property>
  <Property name="email" type="string">
    The user's email address.
  </Property>
  <Property name="email_verified_at" type="timestamp">
    The date and time of when the email verification happened.
  </Property>
  <Property name="updated_at" type="timestamp">
    Timestamp of when the user was last updated.
  </Property>
  <Property name="created_at" type="timestamp">
    Timestamp of when the user was registered.
  </Property>
</Properties>

---

## Get user info {{ tag: 'GET', label: '/v1/user' }}

<Row>
  <Col>
  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/user">

    ```json {{ title: 'Body' }}
    {
      // empty
    }
    ```

    ```json {{ title: 'Headers' }}
    {
      "Content-Type": "application/json",
      "Accept": "application/json",
      "X-XSRF-TOKEN": "{{ % Cookie -> XSRF-TOKEN  % }}"
    }

    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
      {
        "id": 2,
        "name": "Nico",
        "email": "test@examaple.com",
        "email_verified_at": null,
        "created_at": "2023-01-03T07:40:21.000000Z",
        "updated_at": "2023-01-03T07:40:21.000000Z"
      }
    ```

  </Col>
</Row>

---
